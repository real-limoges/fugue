name: Deploy

on:
  push:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/ci.yml

  deploy:
    needs: test
    uses: real-limoges/real-complex/.github/workflows/deploy-service.yml@main
    with:
      service_name: fugue
    permissions:
      contents: read
      id-token: write